apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-webclient
  labels:
    app.kubernetes.io/name: webclient
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  selector:
    app.kubernetes.io/name: webclient
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
