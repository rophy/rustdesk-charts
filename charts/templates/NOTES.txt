RustDesk has been deployed.

Components:
  - hbbs (rendezvous server): {{ include "rustdesk.componentName" (dict "context" . "component" "hbbs") }}
  - hbbr (relay server):      {{ include "rustdesk.componentName" (dict "context" . "component" "hbbr") }}
  - webclient:                 {{ include "rustdesk.componentName" (dict "context" . "component" "webclient") }}

To access the web client, expose the webclient service:
  kubectl port-forward svc/{{ include "rustdesk.componentName" (dict "context" . "component" "webclient") }} 8080:80 -n {{ .Release.Namespace }}
  Then open http://localhost:8080

Service ports:
  hbbs: 21118 (WebSocket)
  hbbr: 21119 (WebSocket)
