apiVersion: v1
kind: Service
metadata:
  name: {{ include "rustdesk.componentName" (dict "context" . "component" "hbbr") }}
  labels:
    {{- include "rustdesk.labels" (dict "context" . "component" "hbbr") | nindent 4 }}
spec:
  selector:
    {{- include "rustdesk.selectorLabels" (dict "context" . "component" "hbbr") | nindent 4 }}
  ports:
    - name: ws
      port: 21119
      targetPort: ws
      protocol: TCP
