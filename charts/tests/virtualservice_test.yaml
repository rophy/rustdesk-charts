suite: virtualservice
templates:
  - templates/virtualservice.yaml
tests:
  - it: is not rendered when istio is disabled
    asserts:
      - hasDocuments:
          count: 0

  - it: renders when istio is enabled
    set:
      istio.enabled: true
      istio.hosts:
        - rustdesk.example.com
      istio.gateway: istio-system/default-gateway
    asserts:
      - isKind:
          of: VirtualService
      - equal:
          path: spec.hosts[0]
          value: rustdesk.example.com
      - equal:
          path: spec.gateways[0]
          value: istio-system/default-gateway

  - it: renders multiple hosts
    set:
      istio.enabled: true
      istio.hosts:
        - rustdesk.example.com
        - rustdesk.example.localhost
    asserts:
      - equal:
          path: spec.hosts[0]
          value: rustdesk.example.com
      - equal:
          path: spec.hosts[1]
          value: rustdesk.example.localhost

  - it: renders standard labels
    set:
      istio.enabled: true
    asserts:
      - isNotNull:
          path: metadata.labels["helm.sh/chart"]
      - equal:
          path: metadata.labels["app.kubernetes.io/managed-by"]
          value: Helm

  - it: uses fullname for metadata name
    set:
      istio.enabled: true
      fullnameOverride: myrd
    asserts:
      - equal:
          path: metadata.name
          value: myrd

  - it: routes all traffic to webclient
    set:
      istio.enabled: true
    asserts:
      - equal:
          path: spec.http[0].route[0].destination.host
          value: RELEASE-NAME-rustdesk-webclient
      - equal:
          path: spec.http[0].route[0].destination.port.number
          value: 80
      - isNull:
          path: spec.http[0].match

  - it: uses fullnameOverride in route destination
    set:
      istio.enabled: true
      fullnameOverride: myrd
    asserts:
      - equal:
          path: spec.http[0].route[0].destination.host
          value: myrd-webclient
